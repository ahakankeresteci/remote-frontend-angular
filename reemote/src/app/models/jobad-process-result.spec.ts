import { JobadProcessResult } from './jobad-process-result';

describe('JobadProcessResult', () => {
  it('should create an instance', () => {
    expect(new JobadProcessResult()).toBeTruthy();
  });
});
